import { styled, Button, Box } from '@mui/material';

export const BookButtonBox = styled(Box)`
  font-family: var(--main-font);
`;
export const StyledButton = styled(Button)`
  font-family: var(--main-font);
`;
